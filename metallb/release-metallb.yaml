apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: metallb
  namespace: infrastructure-common
spec:
  releaseName: metallb
  chart:
    spec:
      chart: metallb
      sourceRef:
        kind: HelmRepository
        name: metallb
        namespace: infrastructure-common
      version: "*"
  targetNamespace: infrastructure-common
  interval: 1h0m0s
  install:
    remediation:
      retries: 3
