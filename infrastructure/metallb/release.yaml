apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: metallb
  namespace: infrastructure
spec:
  releaseName: metallb
  chart:
    spec:
      chart: metallb
      sourceRef:
        kind: HelmChart
        name: metallb
        namespace: infrastructure
      version: "*"
  targetNamespace: metallb
  interval: 1h0m0s
  install:
    remediation:
      retries: 3
