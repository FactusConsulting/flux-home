apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: test-tls
  namespace: default
spec:
  secretName: test-tls
  duration: 25000h # 3y
  renewBefore: 360h # 15d
  issuerRef:
    name: ca-issuer
    kind: ClusterIssuer
  commonName: localhost
  dnsNames:
    - rd.local
    - rdapi.local
